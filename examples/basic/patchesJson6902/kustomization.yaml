resources:
- deploy.yaml
- service.yaml

patchesJson6902:
- target:
    version: apps/v1
    kind: Deployment
    name: nginx
  path: deploy-override.json
- target:
    kind: Service
    name: nginx-service
  # You may use yaml in the same structure as json.
  path: service-override.yaml